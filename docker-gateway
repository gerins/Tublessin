FROM golang:alpine AS builder

RUN apk update && apk add --no-cache git

WORKDIR /app

COPY . .
WORKDIR /app/api_gateway

EXPOSE 8080
RUN go build -o binary

FROM alpine
WORKDIR /app/api_gateway
COPY --from=builder /app/api_gateway /app/api_gateway

ENTRYPOINT ["/app/api_gateway/binary"]